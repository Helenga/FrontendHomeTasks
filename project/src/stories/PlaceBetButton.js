import React, { Component } from 'react';
import './index.css';

class PlaceBetButton extends Component {
    render() {
        return (
            <button className={"button"}>
                Place a bet
            </button>
        )
    }
}

export default PlaceBetButton;